<p style="color: green"><%= notice %></p>

  <%= render @cake_stack %>

<div>
  <%= link_to "Edit this cake stack", edit_cake_stack_path(@cake_stack), class: "bg-green-400 hover:bg-green-300 text-white font-bold py-2 px-4 rounded" %> |
  <%= link_to "Back to cake stacks", cake_stacks_path, class: "bg-green-400 hover:bg-green-300 text-white font-bold py-2 px-4 rounded" %>

  <%= button_to "Destroy this cake stack", @cake_stack, method: :delete %>
</div>
